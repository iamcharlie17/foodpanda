spring.application.name=cart-service
server.port=8083

# MongoDB
spring.data.mongodb.uri=mongodb+srv://admin:adminadmin@cluster0.rpejk7l.mongodb.net/cartdb?retryWrites=true&w=majority&appName=Cluster0

# JWT — must match user-service's secret so tokens issued there validate here
jwt.secret=foodpanda-user-service-super-secret-key-2024-change-in-production
jwt.expiration=3600000

# Internal service URLs
service.restaurant.url=http://localhost:8082

# Eureka Service Registry
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
